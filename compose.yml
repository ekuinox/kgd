services:
  kgd:
    image: ghcr.io/ekuinox/kgd:latest
    container_name: kgd
    restart: unless-stopped
    network_mode: host  # Required for WoL broadcast
    cap_add:
      - NET_RAW  # Required for WoL packets
    volumes:
      - ./config.toml:/app/config.toml:ro

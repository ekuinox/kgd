# Cloudflare の名前付きトンネル用コネクタトークン。
# Cloudflare Zero Trust ダッシュボードでトンネルを作成すると発行される。
# このファイルを .env としてコピーし、実際の値を入れる (.env は git 管理外)。
#
# トンネルを使わない場合は tunnel プロファイルを起動しなければよく、
# その場合この値は不要。
CLOUDFLARE_TUNNEL_TOKEN=
